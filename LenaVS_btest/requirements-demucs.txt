# =========================================================
# LenaVS Backend — dependências do Demucs (Criar instrumental com IA)
# ---------------------------------------------------------
# Instalado no venv /opt/lenavs-demucs pelo Dockerfile.
#
# IMPORTANTE: torch e torchaudio (CPU) são instalados ANTES deste
# arquivo, direto do índice oficial da PyTorch:
#
#   pip install torch==2.5.1 torchaudio==2.5.1 \
#     --index-url https://download.pytorch.org/whl/cpu
#
# torch 2.5.1 (CPU):
#   • demucs 4.1.0 exige torch>=2.1 ✔
#   • fica abaixo do torch 2.6, onde torch.load passou a usar
#     weights_only=True e quebraria o carregamento dos modelos
#     do Demucs
#
# numpy < 2: compatível com o torch 2.5.x (evita erros de ABI
# "compiled against numpy 1.x").
# =========================================================

demucs==4.1.0
numpy>=1.24,<2
