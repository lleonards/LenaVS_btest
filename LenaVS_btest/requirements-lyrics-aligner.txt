# ─── Lyrics Aligner (sincronização automática) ─────────────────────────────
# EXIGE Python 3.12. Instale DENTRO da venv dedicada (nunca no Python do
# sistema) para evitar incompatibilidade de versão:
#
#   bash scripts/setup_aligner_venv.sh        # cria .venv-aligner (3.12)
#
# IMPORTANTE: pinado em 1.0.2. NÃO mudar para 2.0.1 — a API Python mudou
# (a 2.x expõe load_alignment_model; a 1.0.2 usa AlignmentSingleton e
# retorna a palavra na chave "text" — o script scripts/lyrics_aligner.py
# é escrito para a API da 1.0.2).
ctc-forced-aligner==1.0.2
unidecode
soundfile
