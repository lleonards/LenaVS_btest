# Dependências Python do Lyrics Aligner (sincronização automática de letras)
# Requer Python 3.12. O pacote ctc-forced-aligner é FIXADO na versão 1.0.2
# (NÃO usar a versão 2.x — a API é diferente).
#
# Observações:
#  - O pacote ctc-forced-aligner 1.0.2 compila uma extensão C++ durante o pip
#    install; o servidor precisa de um compilador (g++ / build-essential).
#  - O alinhamento usa a trilha PyTorch/torchaudio com o modelo MMS_FA
#    (multilíngue, cobre português, licença MIT do Meta AI).
#  - No primeiro uso, o modelo MMS_FA é baixado automaticamente (~1 GB).
#  - É necessário ffmpeg instalado para decodificar o áudio.
--extra-index-url https://download.pytorch.org/whl/cpu
ctc-forced-aligner==1.0.2
torch
torchaudio
